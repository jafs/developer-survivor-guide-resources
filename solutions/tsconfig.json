{
  // Modo estricto, como el que usa Deno por defecto y el del libro.
  "compilerOptions": {
    "target": "es2024",
    "module": "nodenext",
    "moduleDetection": "force",
    "types": ["node"],
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true
  }
}
